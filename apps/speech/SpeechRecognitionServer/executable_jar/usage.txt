java -jar SpeechRecognitionServer.jar server-port-number

Example - 
java -jar SpeechRecognitionServer.jar 9001

IMPORTANT - The models and config directory should be present in the directory
that has the executable jar file. 

TODO 
1. Make sure the temp audio files are deleted. 
